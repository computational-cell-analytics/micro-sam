name: sam
channels:
    - conda-forge
dependencies:
    - imagecodecs
    - nifty >=1.2.1
    - magicgui
    - napari >=0.5.0
    - natsort
    - pip
    - pooch
    - pyqt
    - python-xxhash
    - python-elf >=0.4.8
    # This does not work for me.
    # - pytorch >=2.4 "libtorch=*=cuda*"
    - pytorch-gpu
    - segment-anything
    - torchvision
    - torch_em >=0.7.0
    - tqdm
    - timm
    - pip:
        - git+https://github.com/ChaoningZhang/MobileSAM.git
