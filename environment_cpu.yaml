name: sam
channels:
    - conda-forge
dependencies:
    - nifty >=1.2.1
    - imagecodecs
    - magicgui
    - napari >=0.5.0
    - natsort
    - pip
    - pooch
    - pyqt
    - python-xxhash
    - python-elf >=0.4.8
    # This does not work for me.
    # - "pytorch=*=cpu"
    - pytorch-cpu
    - segment-anything
    - torchvision
    - torch_em >=0.7.0
    - tqdm
    - timm
    - pip:
        - git+https://github.com/ChaoningZhang/MobileSAM.git
